# @dnosaj/feedback-widget

Drop-in floating feedback widget for React prototypes. Full README lands in the final task of the implementation plan — this stub exists so the initial commit has something human-readable.

Part of a two-repo pair:

- `dnosaj/feedback-widget` (this repo) — the npm widget
- `eijasongai/feedback-service` — the backend + admin UI
